name = "overcast"
main = "./index.js"
compatibility_date = "2023-01-01"

[vars]
# Optional: You can set default variables here, but we're using secrets for the token

[env.production]
name = "overcast-production"

[vars]
# Environment-specific variables

[env.staging]
name = "overcast-staging"

# Note: The actual API token should be set as a secret, not in this file.
# To add the secret, run:
#   wrangler secret put CLOUDFLARE_API_TOKEN