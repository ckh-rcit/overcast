{
	"name": "overcast",
	"version": "1.0.0",
	"description": "Cloudflare zone configuration management tool",
	"scripts": {
		"dev": "wrangler pages dev frontend",
		"deploy": "wrangler pages deploy frontend --project-name overcast",
		"deploy:production": "wrangler pages deploy frontend --project-name overcast --branch production"
	},
	"devDependencies": {
		"wrangler": "^4.74.0"
	}
}
