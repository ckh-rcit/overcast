{
	"name": "overcast",
	"version": "1.0.0",
	"description": "Cloudflare zone configuration management tool",
	"scripts": {
		"dev": "wrangler pages dev .",
		"deploy": "wrangler pages deploy . --project-name overcast",
		"deploy:production": "wrangler pages deploy . --project-name overcast --branch production"
	},
	"devDependencies": {
		"wrangler": "^4.74.0"
	}
}
